namespace Asteroids.Player
{
    public class PlayerFactory
    {
        //public Player Create(Transform transform)
        //{
        //    var mover = new FollowMover(transform);
        //    var moveController = new FollowMoveController(mover, 1f);
        //    var hitDetector = new HitDetector(GroupType.Enemy);
        //    var destroyActions = new IAction[]
        //    {
        //        new ParticleAction(_effectsManager, transform,  EffectType.DeathBig),
        //        new StatAction(_playerStat, StatType.DestroyAsteroid)
        //    };
        //    return new UFO2(moveController, hitDetector, destroyActions);
        //}
    }
}